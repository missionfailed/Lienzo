numero fibonacci(numero i) {
    si i < 2
        regresar 1;
    regresar fibonacci(i - 1) + fibonacci(i - 2);
}

numero factorial(numero k) {
    si k < 2
        regresar 1;
    regresar k * factorial(k - 1);
}

imprimir factorial(4);